import React from "react"

const NavbarProps = (props) =>{
    return(
        <>
        <h2>Hello, {props.logo}</h2>
        <h3>Hello, {props.navigation}</h3>
        </>
    )
    }
    

export default NavbarProps;